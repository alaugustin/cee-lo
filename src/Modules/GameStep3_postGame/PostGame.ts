import { HideGameScreen, ShowGameScreen } from '../Global';

export function PostGame() {
  // ShowGameScreen('CONTAINER_DIV_TBD');
  console.log(`
    - anounce game winner
      - restart game
  `);
  // HideGameScreen('CONTAINER_DIV_TBD');
}
